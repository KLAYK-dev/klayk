# 🎉 Drizzle MCP Server для KLAYK - Налаштування завершено!

> **Дата:** 15 січня 2026 року  
> **Статус:** ✅ Готово до запуску  
> **Проект:** KLAYK Marketplace - Database Package  

---

## 📌 Що таке MCP Server?

**MCP** (Model Context Protocol) - це протокол, який дозволяє AI-агентам (Claude, Cursor) напряму взаємодіяти з вашою базою даних Drizzle ORM. 

Замість того, щоб показувати AI весь код і просити його угадувати, MCP дозволяє AI:
- ✅ Бачити структуру БД напряму
- ✅ Виконувати SQL запити (безпечно)
- ✅ Генерувати міграції через drizzle-kit
- ✅ Аналізувати дані за вашим запитом

---

## 🚀 Швидкий старт (3 кроки)

### Крок 1: Встановіть DATABASE_URL

**На Windows (PowerShell):**
```powershell
$env:DATABASE_URL = "postgresql://localhost:5432/klayk_dev"
```

**На macOS/Linux (Bash):**
```bash
export DATABASE_URL="postgresql://localhost:5432/klayk_dev"
```

**Або постійно (додайте у файл):**
```bash
# packages/database/.env
DATABASE_URL=postgresql://localhost:5432/klayk_dev
```

### Крок 2: Запустіть MCP сервер

**Варіант A: Прямо через npm (найпростіше)**
```bash
cd packages/database
npm run db:mcp:dev
```

**Варіант B: Через Node.js команди**
```bash
# На Windows
node ../../../mcp-commands.js start-dev

# На Unix/macOS
node ../../mcp-commands.js start-dev
```

**Варіант C: Автоматизований скрипт**
```bash
# На Windows
.\mcp-setup.ps1 -Start -Debug

# На Unix/macOS
./mcp-setup.sh --start --debug
```

### Крок 3: Інтегруйте з Claude Desktop

1. Відкрийте конфіг Claude:
   ```
   Windows:  %APPDATA%\Claude\claude_desktop_config.json
   macOS:    ~/Library/Application\ Support/Claude/claude_desktop_config.json
   ```

2. Додайте цей код (замініть `/absolute/path/` на ваш реальний шлях):
   ```json
   {
     "mcpServers": {
       "drizzle": {
         "command": "npx",
         "args": [
           "-y",
           "github:defrex/drizzle-mcp",
           "--config",
           "/absolute/path/to/klayk/packages/database/drizzle.config.ts"
         ],
         "env": {
           "DATABASE_URL": "postgresql://localhost:5432/klayk_dev"
         }
       }
     }
   }
   ```

3. **Перезавантажте Claude Desktop** (закрийте цілком і відкрийте заново)

4. Перевірте: ⚙️ Settings → Developer → MCP Servers → `drizzle` повинен бути **Connected** ✓

---

## 📊 Команди для запуску

| Команда | Опис |
|---------|------|
| `npm run db:mcp` | Запуск MCP-сервера (production) |
| `npm run db:mcp:dev` | Запуск з DEBUG-інформацією |
| `npm run db:studio` | Drizzle Studio (веб-інтерфейс) |
| `npm run db:generate` | Генерувати міграцію |
| `npm run db:push` | Застосувати міграції |

**Запуск з Node.js:**
```bash
node mcp-commands.js verify    # Перевірка налаштування
node mcp-commands.js start     # Запуск
node mcp-commands.js start-dev # З DEBUG
node mcp-commands.js docs      # Показати документацію
```

---

## 📚 Документація

### 🔍 Для тих, хто першого разу:
**→ Прочитайте:** `packages/database/MCP_SETUP_GUIDE.md`
- Детальний виклад усіх 8 частин налаштування
- Рекомендується для повного розуміння

### ✅ Для перевірки налаштування:
**→ Прочитайте:** `packages/database/MCP_VERIFICATION_CHECKLIST.md`
- Крок-за-кроком перевірка
- Troubleshooting рішення для проблем

### 📋 Для швидкого огляду:
**→ Прочитайте:** `packages/database/MCP_SETUP_COMPLETE.md`
- Резюме того, що було зроблено
- Практичні приклади

---

## 💡 Приклади використання

### Приклад 1: Запит до AI про структуру БД

```
Ви (в Claude):
"Скільки таблиць у моїй базі даних? Покажи мені структуру таблиці users"

Claude (з MCP доступом):
✓ Виконає get_schema_info
✓ Покажет: "У вас 14 таблиць. Таблиця users містить:
  - id (uuid, primary key)
  - email (varchar, unique)
  - password_hash (varchar)
  - created_at (timestamp)
  - updated_at (timestamp)
  [та інші...]"
```

### Приклад 2: Аналіз даних

```
Ви: "Скільки замовлень в статусі 'pending' більше як 7 днів?
     Це звучить як проблема - можемо це поправити?"

Claude (з MCP доступом):
✓ Виконає SQL запит:
  SELECT COUNT(*) FROM orders 
  WHERE status='pending' AND created_at < NOW() - INTERVAL '7 days'
✓ Результат: "18 замовлень"
✓ Пропозиція: "Додайте índex на (status, created_at) для швидких запитів"
```

### Приклад 3: Генерація міграції

```
Ви: "Додай таблицю для логування API помилок. 
     Потрібні поля: endpoint, method, status_code, error_message, created_at"

Claude (з MCP доступом):
✓ Спроектує таблицю
✓ Виконає drizzle-kit generate
✓ Генерує файл міграції: src/migrations/20260115_api_logs.sql
✓ Готово до: npm run db:push
```

---

## 🔧 Файли, що були створені/змінені

```
✅ Скрипти для запуску:
  └─ mcp-commands.js           (Node.js команди)
  └─ mcp-setup.ps1             (Windows PowerShell)
  └─ mcp-setup.sh              (Unix/macOS Bash)

✅ Документація:
  └─ packages/database/MCP_SETUP_GUIDE.md              (8-частинний посібник)
  └─ packages/database/MCP_VERIFICATION_CHECKLIST.md   (крок-за-кроком)
  └─ packages/database/MCP_SETUP_COMPLETE.md           (резюме)

✅ Конфігурація:
  └─ .claude-mcp-config.json                (для Claude Desktop)
  └─ packages/database/.mcp-config.json     (MCP сервер конфіг)
  └─ packages/database/package.json         (оновлені скрипти)

✅ Существуючі файли (не змінювалися):
  └─ packages/database/drizzle.config.ts    (уже готовий)
  └─ packages/database/src/schema/          (14 схем готові)
  └─ packages/database/src/migrations/      (готові до застосування)
```

---

## ✨ Перевірка, що все працює

### Тест 1: Локальна діагностика
```bash
node mcp-commands.js verify
```

Очікуються:
- ✓ Node.js version
- ✓ drizzle.config.ts exists
- ✓ Schema files: 14 found
- ✓ DATABASE_URL status

### Тест 2: Запуск сервера
```bash
npm run db:mcp:dev
```

Очікуються повідомлення:
```
✓ Drizzle MCP Server initialized
✓ Schema loaded: 14 tables
✓ Server listening on stdio
```

### Тест 3: У Claude Desktop
1. Натисніть ⚙️ Settings
2. Перейдіть у **Developer** → **MCP Servers**
3. Повинен бути **drizzle** в статусі **Connected** ✓

### Тест 4: Попробуйте запит
```
Ви: "How many tables does my database have? List them."

Claude повинен повернути список усіх 14 таблиць
```

---

## 🚨 Якщо щось не працює

### Проблема 1: "DATABASE_URL is not set"
```bash
# Встановіть вручну:
$env:DATABASE_URL = "postgresql://localhost:5432/klayk_dev"

# Або додайте у .env:
# packages/database/.env
DATABASE_URL=postgresql://localhost:5432/klayk_dev
```

### Проблема 2: "Cannot connect to database"
```bash
# Переконайтеся, що PostgreSQL запущена:
psql --version

# Або перевірте конкретне підключення:
psql "postgresql://localhost:5432/klayk_dev" -c "SELECT 1;"
```

### Проблема 3: Claude не бачит MCP сервер
1. Перевірте синтаксис JSON у конфігу Claude
2. Переконайтеся, що шляхи абсолютні (наприклад `C:/Users/...`)
3. Перезавантажте Claude Desktop повністю
4. Перевірте наявність інших конфліктуючих MCP серверів

**Для детальних рішень див.:** `MCP_VERIFICATION_CHECKLIST.md` → "Debugging & Troubleshooting"

---

## 📞 Наступні кроки

1. ✅ **Налаштування завершено** (ви тут!)
2. 🔄 **Встановіть DATABASE_URL** та протестуйте
3. 🚀 **Запустіть MCP-сервер** локально
4. 🔌 **Інтегруйте з Claude Desktop**
5. 🧪 **Тестуйте функціональність**
6. 🎯 **Використовуйте для розробки**

---

## 📖 Резюме технологій

| Компонент | Технологія | Версія |
|-----------|-----------|--------|
| **ORM** | Drizzle ORM | ^0.52.0 |
| **Kit** | Drizzle Kit | ^0.32.1 |
| **Driver** | postgres.js | ^3.4.8 |
| **DB** | PostgreSQL | 12+ |
| **Protocol** | MCP (Model Context Protocol) | Latest |
| **Runtime** | Node.js | >=24 |

---

## 🎓 Що ви отримали

✅ **Локальний MCP-сервер** для взаємодії з БД через AI  
✅ **Три способи запуску** (npm, Node.js скрипти, shell скрипти)  
✅ **Інтеграція з Claude Desktop** для роботи AI з БД  
✅ **Автоматизовані скрипти** для Windows/Unix систем  
✅ **Детальна документація** для кожного кроку  
✅ **Примери використання** для частих задач  
✅ **Troubleshooting гайд** для типових проблем  

---

## ✨ Ready to go! 🚀

Ви готові використовувати AI для аналізу та розвитку вашої бази даних KLAYK!

**Бажаєте почати?**
```bash
# 1. Встановіть DATABASE_URL
# 2. Запустіть:
npm run db:mcp:dev

# 3. Відкрийте Claude Desktop
# 4. Запитайте: "Скільки таблиць у моїй базі?"
```

---

**Документація доступна в папці `packages/database/`**

*Будь-які питання? Див. файли `MCP_*.md` для детального прояснення.*

**Успіхів з вашим маркетплейсом KLAYK! 🎉**
